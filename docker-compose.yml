version: '3'
services:
  fastapi-app:
    build: .
    ports:
      - "8000:8000"
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000
    volumes:
      - data-volume:/app/data

volumes:
  data-volume:
    driver: local

